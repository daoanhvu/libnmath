precision mediump float;
uniform mat4 MVP;
uniform vec3 lightVec;
attribute vec3 position;
attribute vec3 normal;
varying float light;

void main() {
	light = max(dot(normal, lightVec), 0.0) + 0.2;
	gl_Position = MVP * vec4(position, 1.0);
}